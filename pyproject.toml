[project]
name = "mcp-demo"
version = "0.1.0"
description = "A basic Model Context Protocol server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=2.0.0,<3.0.0",
]

[project.scripts]
mcp-demo = "server:main"
