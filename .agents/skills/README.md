# Repository skills

Add each Codex CLI skill in its own subdirectory here, with a `SKILL.md` file
at the root of that subdirectory. For example:

```text
.agents/skills/
└── my-skill/
    └── SKILL.md
```

Codex CLI discovers repository skills from `.agents/skills/` when working in
this repository.
